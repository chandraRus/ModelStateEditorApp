# ModelStateEditorApp
ModelStateEditorApp is a Windows Forms application written in C# (.NET 8.0) that connects to Autodesk Inventor via COM Interop and allows users to programmatically modify and manage model states in the currently active Inventor document.
